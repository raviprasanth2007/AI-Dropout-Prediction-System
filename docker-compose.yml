version: '3.8'

services:

  backend:
    build: ./backend
    container_name: ai_dropout_backend
    environment:
      DATABASE_URL: postgresql://postgres.xnfnwajdnbiboudrwoxe:aidropoutsystemforcollege@aws-1-ap-northeast-2.pooler.supabase.com:5432/postgres?schema=public
      JWT_SECRET: super-secret-jwt-key-change-in-production
      PORT: 5000
      AI_SERVICE_URL: http://ai-service:8000
    ports:
      - "5000:5000"
    restart: always

  ai-service:
    build: ./ai-service
    container_name: ai_dropout_ml
    ports:
      - "8000:8000"
    restart: always

  frontend:
    build: ./frontend
    container_name: ai_dropout_frontend
    ports:
      - "80:80"
    depends_on:
      - backend
    restart: always

